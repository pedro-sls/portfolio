# Used by metadata, sitemap, robots.txt, and social preview URLs.
# Vercel also provides VERCEL_URL automatically, but this should point to the
# final production domain when the portfolio is deployed.
NEXT_PUBLIC_SITE_URL=https://your-domain.com
